/**
 * Sizes
 */
const tiny = 5;
const small = tiny * 2;
const regular = tiny * 3;
const large = regular * 2;
export const MetricsSizes = {
  tiny,
  small,
  regular,
  large,
};

export const FontSize = {
  small: 16,
  regular: 20,
  large: 40,
};
